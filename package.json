{
  "name": "shopify-geo-audit",
  "version": "0.1.0",
  "description": "Zero-config CLI that audits any Shopify storefront for AI-search (GEO) readiness and generates the actual fixes.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "shopify-geo-audit": "bin/cli.js"
  },
  "main": "./dist/index.js",
  "files": [
    "dist",
    "bin",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --out-dir dist",
    "dev": "tsup src/index.ts --format esm --dts --out-dir dist --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "prepare": "npm run build"
  },
  "keywords": [
    "shopify",
    "geo",
    "generative-engine-optimization",
    "ai-search",
    "structured-data",
    "json-ld",
    "llms-txt",
    "schema-org",
    "cli",
    "seo"
  ],
  "author": "Abhishek Chauhan <automationbyabs@gmail.com> (https://ablyon.tech)",
  "license": "MIT",
  "homepage": "https://github.com/builtbyabs/shopify-geo-audit#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/builtbyabs/shopify-geo-audit.git"
  },
  "bugs": {
    "url": "https://github.com/builtbyabs/shopify-geo-audit/issues"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "commander": "^12.0.0",
    "picocolors": "^1.1.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
